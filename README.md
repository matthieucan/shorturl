urlshortener
============

simple url shortener in flask -- just for fun

Installation :

$ cd shorturl
$ virtualenv VENV
$ source VENV/bin/activate
$ pip install flask sqlalchemy
$ python init_db.py
$ python run.py
